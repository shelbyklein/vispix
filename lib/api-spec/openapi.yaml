openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: Team Photo Album API
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: users
    description: User management
  - name: albums
    description: Album operations
  - name: photos
    description: Photo operations
  - name: tags
    description: Tag operations
  - name: categories
    description: Category operations
  - name: collections
    description: Collection operations
  - name: projects
    description: Project operations (user-curated photo buckets)
  - name: assets
    description: Asset library (brand assets and reference works for deliverables)
  - name: ratings
    description: Rating operations
  - name: stats
    description: Aggregation and statistics
  - name: Storage
    description: Object storage upload and serving endpoints.
  - name: admin
    description: Admin-only configuration endpoints.
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      description: Returns server health status
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /users/me:
    get:
      operationId: getMe
      tags: [users]
      summary: Get current user profile
      responses:
        "200":
          description: Current user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "401":
          description: Unauthorized

  /users/me/nav-order:
    patch:
      operationId: updateNavOrder
      tags: [users]
      summary: Save the current user's preferred sidebar nav order
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NavOrderUpdate"
      responses:
        "200":
          description: Updated user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "400":
          description: Invalid payload
        "401":
          description: Unauthorized

  /users:
    get:
      operationId: listUsers
      tags: [users]
      summary: List all users (admin only)
      responses:
        "200":
          description: List of users
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/User"
        "401":
          description: Unauthorized

  /users/{id}/role:
    patch:
      operationId: updateUserRole
      tags: [users]
      summary: Update a user's role (admin only)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UserRoleUpdate"
      responses:
        "200":
          description: Updated user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden

  /albums:
    get:
      operationId: listAlbums
      tags: [albums]
      summary: List all albums
      responses:
        "200":
          description: List of albums
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Album"
    post:
      operationId: createAlbum
      tags: [albums]
      summary: Create a new album
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AlbumInput"
      responses:
        "201":
          description: Created album
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Album"
        "401":
          description: Unauthorized

  /albums/order:
    put:
      operationId: reorderAlbums
      tags: [albums]
      summary: Set the manual card order of albums
      description: Assigns sort positions from the given id order. Ids omitted from the list keep their relative order after the listed ones.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReorderBody"
      responses:
        "200":
          description: Order updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReorderResult"
        "401":
          description: Unauthorized

  /albums/{id}:
    get:
      operationId: getAlbum
      tags: [albums]
      summary: Get a single album by ID
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Album details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Album"
        "404":
          description: Not found
    patch:
      operationId: updateAlbum
      tags: [albums]
      summary: Update an album
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AlbumUpdate"
      responses:
        "200":
          description: Updated album
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Album"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: Not found
    delete:
      operationId: deleteAlbum
      tags: [albums]
      summary: Delete an album
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: Not found

  /albums/{id}/top-rated:
    get:
      operationId: getAlbumTopRated
      tags: [photos]
      summary: Get top-rated photos in an album
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Top-rated photos in this album
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Photo"
        "401":
          description: Unauthorized
        "404":
          description: Not found

  /albums/{id}/cover:
    patch:
      operationId: setAlbumCover
      tags: [albums]
      summary: Set the cover photo for an album
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AlbumCoverUpdate"
      responses:
        "200":
          description: Updated album
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Album"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden

  /albums/{id}/photos:
    get:
      operationId: listAlbumPhotos
      tags: [photos]
      summary: List photos in an album
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: includeHidden
          in: query
          required: false
          schema:
            type: boolean
        - name: limit
          in: query
          required: false
          schema:
            type: integer
        - name: offset
          in: query
          required: false
          schema:
            type: integer
        - name: inCollection
          in: query
          required: false
          schema:
            type: boolean
        - name: hasRating
          in: query
          required: false
          schema:
            type: boolean
        - name: aiStatus
          in: query
          required: false
          schema:
            type: string
            enum: [has_description, failed, not_analysed]
        - name: attributionTagId
          in: query
          required: false
          schema:
            type: integer
          description: Only photos carrying this attribution tag.
        - name: hasAttribution
          in: query
          required: false
          schema:
            type: boolean
          description: true = photos with any attribution tag; false = untagged photos. Ignored when attributionTagId is set.
      responses:
        "200":
          description: Page of photos in album
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListAlbumPhotosPagedResponse"
    post:
      operationId: uploadPhoto
      tags: [photos]
      summary: Add a photo to an album (URL-based)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PhotoUploadInput"
      responses:
        "201":
          description: Created photo
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Photo"
        "401":
          description: Unauthorized
        "404":
          description: Album not found

  /search:
    get:
      operationId: searchPhotos
      tags: [photos]
      summary: Search photos by keyword across caption, album title, and AI description
      parameters:
        - name: q
          in: query
          required: true
          schema:
            type: string
        - name: ratingMin
          in: query
          required: false
          schema:
            type: number
        - name: ratingMax
          in: query
          required: false
          schema:
            type: number
        - name: dateFrom
          in: query
          required: false
          schema:
            type: string
        - name: dateTo
          in: query
          required: false
          schema:
            type: string
        - name: uploaderId
          in: query
          required: false
          schema:
            type: integer
        - name: includeHidden
          in: query
          required: false
          schema:
            type: boolean
        - name: limit
          in: query
          required: false
          schema:
            type: integer
        - name: offset
          in: query
          required: false
          schema:
            type: integer
        - name: exclude
          in: query
          required: false
          description: Terms to exclude — photos whose AI description matches any are dropped.
          schema:
            type: array
            items:
              type: string
      responses:
        "200":
          description: Search results (one page)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SearchPhotosPagedResponse"
        "401":
          description: Unauthorized

  /search/semantic:
    get:
      operationId: semanticSearchPhotos
      tags: [photos]
      summary: Semantic photo search — ranks photos by image-embedding similarity to the query text
      parameters:
        - name: q
          in: query
          required: true
          schema:
            type: string
        - name: topK
          in: query
          required: false
          schema:
            type: integer
        - name: includeHidden
          in: query
          required: false
          schema:
            type: boolean
        - name: exclude
          in: query
          required: false
          description: Concepts to steer away from — the query vector is pushed away from their embedding.
          schema:
            type: array
            items:
              type: string
      responses:
        "200":
          description: Photos ranked by semantic similarity (most similar first)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Photo"
        "401":
          description: Unauthorized

  /photos/{id}/similar:
    get:
      operationId: listSimilarPhotos
      tags: [photos]
      summary: Photos most visually similar to the given photo (by image embedding)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: topK
          in: query
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: Similar photos, most similar first
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Photo"
        "401":
          description: Unauthorized
        "404":
          description: Not found

  /photos:
    get:
      operationId: listPhotos
      tags: [photos]
      summary: List all photos with optional filters
      parameters:
        - name: search
          in: query
          required: false
          schema:
            type: string
        - name: tag
          in: query
          required: false
          schema:
            type: string
        - name: categoryId
          in: query
          required: false
          schema:
            type: integer
        - name: ratingMin
          in: query
          required: false
          schema:
            type: number
        - name: ratingMax
          in: query
          required: false
          schema:
            type: number
        - name: dateFrom
          in: query
          required: false
          schema:
            type: string
        - name: dateTo
          in: query
          required: false
          schema:
            type: string
        - name: uploaderId
          in: query
          required: false
          schema:
            type: integer
        - name: includeHidden
          in: query
          required: false
          schema:
            type: boolean
        - name: albumId
          in: query
          required: false
          schema:
            type: integer
        - name: aiStatus
          in: query
          required: false
          schema:
            type: string
            enum: ["has_description", "failed", "not_analysed"]
        - name: attributionTagId
          in: query
          required: false
          schema:
            type: integer
          description: Only photos carrying this attribution tag.
        - name: hasAttribution
          in: query
          required: false
          schema:
            type: boolean
          description: true = photos with any attribution tag; false = untagged photos. Ignored when attributionTagId is set.
        - name: limit
          in: query
          required: false
          schema:
            type: integer
        - name: offset
          in: query
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: List of photos (one page)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListPhotosPagedResponse"

  /photos/bulk:
    patch:
      operationId: bulkUpdatePhotos
      tags: [photos]
      summary: Bulk update photos (admin only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BulkPhotoUpdate"
      responses:
        "200":
          description: Number of photos updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BulkPhotoUpdateResult"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
    delete:
      operationId: bulkDeletePhotos
      tags: [photos]
      summary: Bulk delete photos (admin only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BulkPhotoDelete"
      responses:
        "200":
          description: Number of photos deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BulkPhotoDeleteResult"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden

  /attribution-tags:
    get:
      operationId: listAttributionTags
      tags: [attribution]
      summary: List attribution / usage-rights tags
      responses:
        "200":
          description: All attribution tags, ordered by name
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AttributionTag"
        "401":
          description: Unauthorized
    post:
      operationId: createAttributionTag
      tags: [attribution]
      summary: Create an attribution tag (admin only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AttributionTagInput"
      responses:
        "201":
          description: Created tag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AttributionTag"
        "400":
          description: Invalid name
        "409":
          description: Name already exists

  /attribution-tags/{id}:
    patch:
      operationId: updateAttributionTag
      tags: [attribution]
      summary: Rename an attribution tag (admin only)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AttributionTagInput"
      responses:
        "200":
          description: Updated tag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AttributionTag"
        "404":
          description: Tag not found
    delete:
      operationId: deleteAttributionTag
      tags: [attribution]
      summary: Delete an attribution tag (admin only)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted
        "404":
          description: Tag not found

  /albums/{id}/attribution-tags:
    get:
      operationId: getAlbumAttributionSummary
      tags: [attribution]
      summary: Per-tag counts of how many of an album's photos carry each attribution tag
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Album attribution coverage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AlbumAttributionSummary"
        "404":
          description: Album not found
    post:
      operationId: setAlbumAttribution
      tags: [attribution]
      summary: Add or remove one attribution tag on every photo in an album (admin only)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AlbumAttributionUpdate"
      responses:
        "200":
          description: Number of photo-tag links changed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BulkAttributionTagsResult"
        "404":
          description: Album or tag not found

  /photos/attribution-tags/bulk:
    post:
      operationId: bulkSetAttributionTags
      tags: [attribution]
      summary: Add or remove one attribution tag on many photos (admin only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BulkAttributionTags"
      responses:
        "200":
          description: Number of photo-tag links changed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BulkAttributionTagsResult"
        "404":
          description: Tag not found

  /photos/{id}/attribution-tags:
    post:
      operationId: addPhotoAttributionTag
      tags: [attribution]
      summary: Tag a photo with an attribution tag
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PhotoAttributionTagInput"
      responses:
        "204":
          description: Tagged
        "404":
          description: Photo or tag not found

  /photos/{id}/attribution-tags/{tagId}:
    delete:
      operationId: removePhotoAttributionTag
      tags: [attribution]
      summary: Remove an attribution tag from a photo
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: tagId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Removed

  /photos/{id}:
    get:
      operationId: getPhoto
      tags: [photos]
      summary: Get a single photo by ID
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Photo details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Photo"
        "404":
          description: Not found
    patch:
      operationId: updatePhoto
      tags: [photos]
      summary: Update photo metadata
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PhotoUpdate"
      responses:
        "200":
          description: Updated photo
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Photo"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
    delete:
      operationId: deletePhoto
      tags: [photos]
      summary: Delete a photo
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted
        "401":
          description: Unauthorized
        "403":
          description: Forbidden

  /photos/{id}/tags:
    post:
      operationId: addPhotoTag
      tags: [tags]
      summary: Add a tag to a photo (creates tag if needed)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PhotoTagInput"
      responses:
        "200":
          description: Updated photo
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Photo"
        "401":
          description: Unauthorized

  /photos/{id}/tags/{tagId}:
    delete:
      operationId: removePhotoTag
      tags: [tags]
      summary: Remove a tag from a photo
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: tagId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Updated photo
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Photo"
        "401":
          description: Unauthorized

  /photos/{id}/categories:
    post:
      operationId: addPhotoCategory
      tags: [categories]
      summary: Add a category to a photo
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PhotoCategoryInput"
      responses:
        "200":
          description: Updated photo
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Photo"
        "401":
          description: Unauthorized

  /photos/{id}/categories/{categoryId}:
    delete:
      operationId: removePhotoCategory
      tags: [categories]
      summary: Remove a category from a photo
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: categoryId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Updated photo
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Photo"
        "401":
          description: Unauthorized

  /photos/{id}/rating:
    post:
      operationId: ratePhoto
      tags: [ratings]
      summary: Rate a photo (1-5 stars)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RatingInput"
      responses:
        "200":
          description: Updated photo with new average rating
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Photo"
        "401":
          description: Unauthorized
    delete:
      operationId: clearPhotoRating
      tags: [ratings]
      summary: Clear the current user's rating on a photo
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Updated photo with rating removed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Photo"
        "401":
          description: Unauthorized
        "404":
          description: Photo not found

  /tags:
    get:
      operationId: listTags
      tags: [tags]
      summary: List all tags
      responses:
        "200":
          description: List of tags
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Tag"
    post:
      operationId: createTag
      tags: [tags]
      summary: Create a new tag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TagInput"
      responses:
        "201":
          description: Created tag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Tag"
        "401":
          description: Unauthorized

  /categories:
    get:
      operationId: listCategories
      tags: [categories]
      summary: List all categories
      responses:
        "200":
          description: List of categories
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Category"
    post:
      operationId: createCategory
      tags: [categories]
      summary: Create a new category (admin only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CategoryInput"
      responses:
        "201":
          description: Created category
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Category"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden

  /categories/{id}:
    delete:
      operationId: deleteCategory
      tags: [categories]
      summary: Delete a category (admin only)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted
        "401":
          description: Unauthorized
        "403":
          description: Forbidden

  /registration-settings:
    get:
      operationId: getRegistrationSettings
      tags: [admin]
      summary: Get registration settings (public)
      description: Returns whether public self-registration is currently enabled. No authentication required.
      responses:
        "200":
          description: Current registration settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RegistrationSettings"

  /admin/registration-settings:
    patch:
      operationId: updateRegistrationSettings
      tags: [admin]
      summary: Update registration settings (admin only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegistrationSettingsUpdate"
      responses:
        "200":
          description: Updated registration settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RegistrationSettings"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden

  /admin/ai-settings:
    get:
      operationId: getAiSettings
      tags: [admin]
      summary: Get AI provider settings (admin only)
      responses:
        "200":
          description: Current AI settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiSettings"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
    patch:
      operationId: updateAiSettings
      tags: [admin]
      summary: Update AI enabled flag and active provider (admin only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AiSettingsUpdate"
      responses:
        "200":
          description: Updated AI settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiSettings"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden

  /admin/ai-settings/providers/{provider}/key:
    put:
      operationId: setAiProviderKey
      tags: [admin]
      summary: Save (or replace) the API key for a provider (admin only)
      parameters:
        - name: provider
          in: path
          required: true
          schema:
            type: string
            enum: [openai, anthropic, gemini]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AiProviderKeyInput"
      responses:
        "200":
          description: Updated AI settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiSettings"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
    delete:
      operationId: clearAiProviderKey
      tags: [admin]
      summary: Clear the saved API key for a provider (admin only)
      parameters:
        - name: provider
          in: path
          required: true
          schema:
            type: string
            enum: [openai, anthropic, gemini]
      responses:
        "200":
          description: Updated AI settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiSettings"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden

  /admin/ai-analysis-events:
    get:
      operationId: listAiAnalysisEvents
      tags: [admin]
      summary: List recent AI photo analysis events (admin only)
      responses:
        "200":
          description: Recent AI analysis events
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AiAnalysisEvent"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden

  /admin/ai-analysis-events/retry-all:
    post:
      operationId: bulkRetryAiAnalysisEvents
      tags: [admin]
      summary: Re-run AI analysis for all recently failed events (admin only)
      responses:
        "200":
          description: Bulk retry result summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BulkRetryAiAnalysisEventsResult"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden

  /admin/ai-analysis-events/{id}/retry:
    post:
      operationId: retryAiAnalysisEvent
      tags: [admin]
      summary: Re-run AI analysis for the photo of a failed event (admin only)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: New AI analysis event recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiAnalysisEvent"
        "400":
          description: Event is not in a retryable state
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: Event or its photo not found

  /admin/photos/duplicates:
    get:
      operationId: listDuplicatePhotoGroups
      tags: [admin]
      summary: List groups of exact-duplicate photos sharing a content hash (admin only)
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
          description: Max groups per page (default 20, capped at 100).
        - name: offset
          in: query
          required: false
          schema:
            type: integer
          description: Number of groups to skip (hash-ordered).
      responses:
        "200":
          description: One page of duplicate photo groups (only groups with 2+ members)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListDuplicatePhotoGroupsResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden

  /admin/photos/duplicates/summary:
    get:
      operationId: getDuplicatesSummary
      tags: [admin]
      summary: Lightweight duplicate counts for the admin summary (admin only)
      responses:
        "200":
          description: Number of duplicate groups and deletable extra copies
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DuplicatesSummaryResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden

  /admin/photos/duplicates/delete-extras:
    post:
      operationId: deleteDuplicateExtras
      tags: [admin]
      summary: Delete every extra duplicate copy, keeping covers or one photo per group (admin only)
      responses:
        "200":
          description: Number of photos deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeleteDuplicateExtrasResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden

  /stats/dashboard:
    get:
      operationId: getDashboardStats
      tags: [stats]
      summary: Get dashboard summary statistics
      responses:
        "200":
          description: Dashboard statistics
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardStats"
        "401":
          description: Unauthorized

  /stats/recent-photos:
    get:
      operationId: getRecentPhotos
      tags: [stats]
      summary: Get recently uploaded photos across all albums
      responses:
        "200":
          description: Recent photos
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Photo"
        "401":
          description: Unauthorized

  /stats/top-rated:
    get:
      operationId: getTopRatedPhotos
      tags: [stats]
      summary: Get top-rated photos across all albums
      responses:
        "200":
          description: Top rated photos
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Photo"
        "401":
          description: Unauthorized

  /storage/uploads/request-url:
    post:
      tags: [Storage]
      operationId: requestUploadUrl
      summary: Request a presigned URL for file upload
      description: |
        Returns a presigned GCS URL for direct upload. The client sends JSON
        metadata here, then uploads the file directly to the returned URL.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UploadUrlRequest'
      responses:
        '200':
          description: Presigned upload URL generated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UploadUrlResponse'
        '400':
          description: Missing or invalid required fields.
        '500':
          description: Failed to generate upload URL.

  /collections:
    get:
      operationId: listCollections
      tags: [collections]
      summary: List collections of one kind
      parameters:
        - name: kind
          in: query
          required: false
          description: Which kind to list. Defaults to 'collection', so existing consumers never see people.
          schema:
            type: string
            enum: [collection, person]
      responses:
        "200":
          description: List of collections
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CollectionSummary"
        "401":
          description: Unauthorized
    post:
      operationId: createCollection
      tags: [collections]
      summary: Create a new collection
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CollectionInput"
      responses:
        "201":
          description: Created collection
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Collection"
        "401":
          description: Unauthorized

  /collections/order:
    put:
      operationId: reorderCollections
      tags: [collections]
      summary: Set the manual card order of collections
      description: Assigns sort positions from the given id order. Shared by the Collections and Smart Collections pages (same rows).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReorderBody"
      responses:
        "200":
          description: Order updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReorderResult"
        "401":
          description: Unauthorized

  /collections/{id}:
    get:
      operationId: getCollection
      tags: [collections]
      summary: Get a single collection with its photos
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Collection with photos
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Collection"
        "404":
          description: Not found
    patch:
      operationId: updateCollection
      tags: [collections]
      summary: Update a collection (owner or admin)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CollectionUpdate"
      responses:
        "200":
          description: Updated collection
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Collection"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: Not found
    delete:
      operationId: deleteCollection
      tags: [collections]
      summary: Delete a collection (owner or admin)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: Not found

  /collections/{id}/photos:
    post:
      operationId: addPhotoToCollection
      tags: [collections]
      summary: Add a photo to a collection
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CollectionPhotoInput"
      responses:
        "204":
          description: Added
        "401":
          description: Unauthorized
        "404":
          description: Not found

  /collections/{id}/photos/{photoId}:
    delete:
      operationId: removePhotoFromCollection
      tags: [collections]
      summary: Remove a photo from a collection
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: photoId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Removed
        "401":
          description: Unauthorized
        "404":
          description: Not found

  /collections/{id}/negative-photos:
    get:
      operationId: listCollectionNegativePhotos
      tags: [collections]
      summary: Photos marked "not applicable" to a smart collection (negative examples)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Negative-example photos
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Photo"
        "401":
          description: Unauthorized
        "404":
          description: Not found
    post:
      operationId: addNegativePhotoToCollection
      tags: [collections]
      summary: Mark a photo as not applicable to a collection (removes it as a positive member)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CollectionPhotoInput"
      responses:
        "204":
          description: Marked
        "401":
          description: Unauthorized
        "404":
          description: Not found

  /collections/{id}/negative-photos/{photoId}:
    delete:
      operationId: removeNegativePhotoFromCollection
      tags: [collections]
      summary: Un-mark a photo as not applicable
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: photoId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Un-marked
        "401":
          description: Unauthorized
        "404":
          description: Not found

  /collections/{id}/cover:
    patch:
      operationId: setCollectionCover
      tags: [collections]
      summary: Set the cover photo for a collection
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CollectionCoverUpdate"
      responses:
        "200":
          description: Updated collection
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Collection"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: Not found

  /collections/{id}/smart-photos:
    get:
      operationId: getSmartCollectionPhotos
      tags: [collections]
      summary: Photos most similar to a collection — ranked by the average embedding of its members, falling back to its search term when empty
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: topK
          in: query
          required: false
          description: Page size (max 500, default 100).
          schema:
            type: integer
        - name: offset
          in: query
          required: false
          description: Number of ranked results to skip — enables paging through the full ranking. Default 0.
          schema:
            type: integer
      responses:
        "200":
          description: Non-member photos ranked by similarity (most similar first). A page shorter than topK means the ranking is exhausted.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Photo"
        "401":
          description: Unauthorized
        "404":
          description: Not found

  /projects:
    get:
      operationId: listProjects
      tags: [projects]
      summary: List all projects
      responses:
        "200":
          description: List of projects
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ProjectSummary"
        "401":
          description: Unauthorized
    post:
      operationId: createProject
      tags: [projects]
      summary: Create a new project
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProjectInput"
      responses:
        "201":
          description: Created project
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Project"
        "401":
          description: Unauthorized

  /projects/{id}/download:
    get:
      operationId: downloadProjectPhotos
      tags: [projects]
      summary: Download all of a project's photos as a zip archive (streamed)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Zip archive of the project's original photo files
          content:
            application/zip:
              schema:
                type: string
                format: binary
        "401":
          description: Unauthorized
        "404":
          description: Project not found or has no photos

  /projects/order:
    put:
      operationId: reorderProjects
      tags: [projects]
      summary: Set the manual card order of projects
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReorderBody"
      responses:
        "200":
          description: Order updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReorderResult"
        "401":
          description: Unauthorized

  /projects/{id}:
    get:
      operationId: getProject
      tags: [projects]
      summary: Get a single project with its photos
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Project with photos
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Project"
        "404":
          description: Not found
    patch:
      operationId: updateProject
      tags: [projects]
      summary: Update a project (owner or admin)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProjectUpdate"
      responses:
        "200":
          description: Updated project
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Project"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: Not found
    delete:
      operationId: deleteProject
      tags: [projects]
      summary: Delete a project (owner or admin)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: Not found

  /projects/{id}/photos:
    post:
      operationId: addPhotoToProject
      tags: [projects]
      summary: Add a photo to a project
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProjectPhotoInput"
      responses:
        "204":
          description: Added
        "401":
          description: Unauthorized
        "404":
          description: Not found

  /projects/{id}/photos/{photoId}:
    delete:
      operationId: removePhotoFromProject
      tags: [projects]
      summary: Remove a photo from a project
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: photoId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Removed
        "401":
          description: Unauthorized
        "404":
          description: Not found

  /assets:
    get:
      operationId: listAssets
      tags: [assets]
      summary: List asset library items (brand assets and reference works)
      parameters:
        - name: kind
          in: query
          required: false
          schema:
            type: string
            enum: [brand, reference]
        - name: projectId
          in: query
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: List of assets
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Asset"
        "401":
          description: Unauthorized
    post:
      operationId: createAsset
      tags: [assets]
      summary: Register an uploaded file as an asset (upload the file via the storage request-url flow first)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetInput"
      responses:
        "201":
          description: Created asset
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Asset"
        "400":
          description: Invalid input
        "401":
          description: Unauthorized

  /assets/{id}:
    patch:
      operationId: updateAsset
      tags: [assets]
      summary: Update an asset's metadata (owner or admin)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetUpdate"
      responses:
        "200":
          description: Updated asset
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Asset"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: Not found
    delete:
      operationId: deleteAsset
      tags: [assets]
      summary: Delete an asset (owner or admin)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: Not found

  /photos/{id}/suggestions/{collectionId}/accept:
    post:
      operationId: acceptPhotoSuggestion
      tags: [photos]
      summary: Accept a suggested collection for a photo (adds photo to the collection)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: collectionId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Updated photo
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Photo"
        "401":
          description: Unauthorized
        "404":
          description: Not found

  /photos/{id}/suggestions/{collectionId}/dismiss:
    post:
      operationId: dismissPhotoSuggestion
      tags: [photos]
      summary: Dismiss a suggested collection for a photo
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: collectionId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Updated photo
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Photo"
        "401":
          description: Unauthorized
        "404":
          description: Not found

  /photos/{id}/tag-suggestions/{tagName}/accept:
    post:
      operationId: acceptPhotoTagSuggestion
      tags: [photos]
      summary: Accept a suggested tag for a photo (adds the tag to the photo)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: tagName
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Updated photo
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Photo"
        "401":
          description: Unauthorized
        "404":
          description: Not found

  /photos/{id}/tag-suggestions/{tagName}/dismiss:
    post:
      operationId: dismissPhotoTagSuggestion
      tags: [photos]
      summary: Dismiss a suggested tag for a photo
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: tagName
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Updated photo
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Photo"
        "401":
          description: Unauthorized
        "404":
          description: Not found

  /photos/{id}/category-suggestions/{categoryId}/accept:
    post:
      operationId: acceptPhotoCategorySuggestion
      tags: [photos]
      summary: Accept a suggested category for a photo (links the category to the photo)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: categoryId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Updated photo
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Photo"
        "401":
          description: Unauthorized
        "404":
          description: Not found

  /photos/{id}/category-suggestions/{categoryId}/dismiss:
    post:
      operationId: dismissPhotoCategorySuggestion
      tags: [photos]
      summary: Dismiss a suggested category for a photo
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: categoryId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Updated photo
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Photo"
        "401":
          description: Unauthorized
        "404":
          description: Not found

  /photos/{id}/new-collection-suggestions/{suggestionId}/accept:
    post:
      operationId: acceptPhotoNewCollectionSuggestion
      tags: [photos]
      summary: Accept a suggested new collection for a photo (creates collection and adds photo)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: suggestionId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AcceptNewCollectionSuggestionBody"
      responses:
        "200":
          description: Updated photo
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Photo"
        "401":
          description: Unauthorized
        "404":
          description: Not found

  /photos/{id}/new-collection-suggestions/{suggestionId}/dismiss:
    post:
      operationId: dismissPhotoNewCollectionSuggestion
      tags: [photos]
      summary: Dismiss a suggested new collection for a photo
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: suggestionId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Updated photo
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Photo"
        "401":
          description: Unauthorized
        "404":
          description: Not found

  /photos/{id}/rerun-analysis:
    post:
      operationId: rerunPhotoAnalysis
      tags: [photos]
      summary: Re-run AI analysis for a photo (admin or uploader only)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: New AI analysis event recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiAnalysisEvent"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: Photo not found

  /stats/tag-cloud:
    get:
      operationId: getTagCloud
      tags: [stats]
      summary: Get tag usage counts for tag cloud display
      responses:
        "200":
          description: Tag cloud data
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TagCount"
        "401":
          description: Unauthorized

components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status

    User:
      type: object
      required: [id, authUserId, name, email, role, createdAt]
      properties:
        id:
          type: integer
        authUserId:
          type: string
        name:
          type: string
        email:
          type: string
        role:
          type: string
          enum: [admin, member]
        createdAt:
          type: string
          format: date-time
        navOrder:
          type: ["array", "null"]
          items:
            type: string
          description: Preferred sidebar nav order (nav hrefs). Null = default order.

    NavOrderUpdate:
      type: object
      required: [navOrder]
      properties:
        navOrder:
          type: array
          items:
            type: string
          description: Nav hrefs in the user's preferred order.

    UserRoleUpdate:
      type: object
      required: [role]
      properties:
        role:
          type: string
          enum: [admin, member]

    ReorderBody:
      type: object
      required: [ids]
      properties:
        ids:
          type: array
          description: Entity ids in the desired display order (position = index).
          items:
            type: integer

    ReorderResult:
      type: object
      required: [updated]
      properties:
        updated:
          type: integer
          description: Number of rows whose sort position was set.

    Album:
      type: object
      required: [id, ownerId, title, createdAt, photoCount]
      properties:
        id:
          type: integer
        ownerId:
          type: integer
        ownerName:
          type: ["string", "null"]
        title:
          type: string
        description:
          type: ["string", "null"]
        eventDate:
          type: ["string", "null"]
        folder:
          type: ["string", "null"]
        coverPhotoId:
          type: ["integer", "null"]
        coverPhotoUrl:
          type: ["string", "null"]
        coverPhotoThumbnailKey:
          type: ["string", "null"]
        photoCount:
          type: integer
        ratedCount:
          type: integer
        hiddenCount:
          type: integer
        unratedCount:
          type: integer
        createdAt:
          type: string
          format: date-time

    AlbumInput:
      type: object
      required: [title]
      properties:
        title:
          type: string
          minLength: 1
        description:
          type: string
        eventDate:
          type: string
        folder:
          type: string

    AlbumUpdate:
      type: object
      properties:
        title:
          type: string
          minLength: 1
        description:
          type: string
        eventDate:
          type: ["string", "null"]
        folder:
          type: ["string", "null"]

    AlbumCoverUpdate:
      type: object
      required: [photoId]
      properties:
        photoId:
          type: integer

    CollectionCoverUpdate:
      type: object
      required: [photoId]
      properties:
        photoId:
          type: integer

    DuplicatePhoto:
      type: object
      required: [id, albumId, createdAt, isAlbumCover, collectionCount]
      properties:
        id:
          type: integer
        albumId:
          type: integer
        albumTitle:
          type: ["string", "null"]
        filename:
          type: ["string", "null"]
        thumbnailUrl:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time
        isAlbumCover:
          type: boolean
          description: True if this photo is the cover of its album; deleting it would clear the album cover.
        collectionCount:
          type: integer
          description: Number of collections this photo belongs to.

    DuplicatePhotoGroup:
      type: object
      required: [contentHash, photos]
      properties:
        contentHash:
          type: string
        photos:
          type: array
          items:
            $ref: "#/components/schemas/DuplicatePhoto"

    ListDuplicatePhotoGroupsResponse:
      type: object
      required: [groups, hasMore]
      properties:
        groups:
          type: array
          items:
            $ref: "#/components/schemas/DuplicatePhotoGroup"
        hasMore:
          type: boolean
          description: True when more groups exist beyond this page.

    DuplicatesSummaryResponse:
      type: object
      required: [groupCount, extraCount]
      properties:
        groupCount:
          type: integer
          description: Number of duplicate groups (2+ byte-identical photos).
        extraCount:
          type: integer
          description: Deletable extra copies across all groups (keeping covers, else one per group).

    DeleteDuplicateExtrasResponse:
      type: object
      required: [deleted]
      properties:
        deleted:
          type: integer

    Photo:
      type: object
      required: [id, albumId, uploaderId, url, createdAt, ratingCount, isHidden]
      properties:
        id:
          type: integer
        albumId:
          type: integer
        albumTitle:
          type: ["string", "null"]
        uploaderId:
          type: integer
        storageKey:
          type: ["string", "null"]
        thumbnailKey:
          type: ["string", "null"]
        url:
          type: string
        filename:
          type: ["string", "null"]
        filesize:
          type: ["integer", "null"]
        width:
          type: ["integer", "null"]
          description: Display width in pixels (EXIF-orientation corrected). Null until captured on upload or backfilled.
        height:
          type: ["integer", "null"]
          description: Display height in pixels (EXIF-orientation corrected). Null until captured on upload or backfilled.
        contentHash:
          type: ["string", "null"]
          description: SHA-256 hex digest of the original image bytes, used for exact duplicate detection. Null until computed.
        takenAt:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time
        isHidden:
          type: boolean
        averageRating:
          type: ["number", "null"]
        ratingCount:
          type: integer
        myRating:
          type: ["integer", "null"]
        photoCollections:
          type: array
          items:
            $ref: "#/components/schemas/CollectionSummary"
        photoProjects:
          type: array
          description: Projects this photo currently belongs to (membership only).
          items:
            $ref: "#/components/schemas/PhotoProjectMembership"
        attributionTags:
          type: array
          description: Attribution / usage-rights tags this photo is cleared for.
          items:
            $ref: "#/components/schemas/AttributionTag"
        aiDescription:
          type: ["string", "null"]
        latestAiStatus:
          type: ["string", "null"]
          enum: ["success", "skipped", "failed", null]
          description: Status of the most recent AI analysis event for this photo
        suggestedCollections:
          type: array
          items:
            $ref: "#/components/schemas/SuggestedCollection"
        suggestedNewCollections:
          type: array
          items:
            $ref: "#/components/schemas/SuggestedNewCollection"
        ratings:
          type: array
          items:
            $ref: "#/components/schemas/PhotoRating"

    ListAlbumPhotosPagedResponse:
      type: object
      required: [photos, hasMore]
      properties:
        photos:
          type: array
          items:
            $ref: "#/components/schemas/Photo"
        hasMore:
          type: boolean

    ListPhotosPagedResponse:
      type: object
      required: [photos, hasMore]
      properties:
        photos:
          type: array
          items:
            $ref: "#/components/schemas/Photo"
        hasMore:
          type: boolean

    SearchPhotosPagedResponse:
      type: object
      required: [photos, hasMore]
      properties:
        photos:
          type: array
          items:
            $ref: "#/components/schemas/Photo"
        hasMore:
          type: boolean

    PhotoRating:
      type: object
      required: [userId, score, createdAt]
      properties:
        userId:
          type: integer
        userName:
          type: ["string", "null"]
        score:
          type: integer
        createdAt:
          type: string
          format: date-time

    CollectionSummary:
      type: object
      required: [id, title, createdById, createdAt, photoCount]
      properties:
        id:
          type: integer
        title:
          type: string
        description:
          type: ["string", "null"]
        createdById:
          type: integer
        photoCount:
          type: integer
        coverPhotoId:
          type: ["integer", "null"]
        coverPhotoUrl:
          type: ["string", "null"]
        coverPhotoThumbnailKey:
          type: ["string", "null"]
        sampleThumbnailUrls:
          type: array
          items:
            type: string
          description: Up to 5 random member photo thumbnails (display URLs), re-sampled per request. Feeds crossfading card thumbnails.
        tags:
          type: array
          items:
            type: string
        kind:
          type: string
          enum: [collection, person]
          description: People are collections with kind 'person'.
        createdAt:
          type: string
          format: date-time

    Collection:
      type: object
      required: [id, title, createdById, createdAt, photoCount]
      properties:
        id:
          type: integer
        title:
          type: string
        description:
          type: ["string", "null"]
        createdById:
          type: integer
        photoCount:
          type: integer
        coverPhotoId:
          type: ["integer", "null"]
        coverPhotoUrl:
          type: ["string", "null"]
        smartQuery:
          type: ["string", "null"]
        kind:
          type: string
          enum: [collection, person]
          description: People are collections with kind 'person' — same machinery, listed on their own pages.
        tags:
          type: array
          items:
            type: string
        createdAt:
          type: string
          format: date-time
        photos:
          type: array
          items:
            $ref: "#/components/schemas/Photo"

    CollectionInput:
      type: object
      required: [title]
      properties:
        title:
          type: string
          minLength: 1
        description:
          type: string
        kind:
          type: string
          enum: [collection, person]
          description: Defaults to 'collection'.

    CollectionUpdate:
      type: object
      properties:
        title:
          type: string
          minLength: 1
        description:
          type: ["string", "null"]
        coverPhotoId:
          type: ["integer", "null"]
        smartQuery:
          type: ["string", "null"]

    CollectionPhotoInput:
      type: object
      required: [photoId]
      properties:
        photoId:
          type: integer

    ProjectSummary:
      type: object
      required: [id, name, createdById, createdAt, updatedAt, photoCount]
      properties:
        id:
          type: integer
        name:
          type: string
        description:
          type: ["string", "null"]
        createdById:
          type: integer
        photoCount:
          type: integer
        coverPhotoUrl:
          type: ["string", "null"]
        coverPhotoThumbnailKey:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    Project:
      type: object
      required: [id, name, createdById, createdAt, updatedAt, photoCount]
      properties:
        id:
          type: integer
        name:
          type: string
        description:
          type: ["string", "null"]
        createdById:
          type: integer
        photoCount:
          type: integer
        coverPhotoUrl:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        photos:
          type: array
          items:
            $ref: "#/components/schemas/Photo"

    ProjectInput:
      type: object
      required: [name]
      properties:
        name:
          type: string
          minLength: 1
        description:
          type: string

    ProjectUpdate:
      type: object
      properties:
        name:
          type: string
          minLength: 1
        description:
          type: ["string", "null"]

    ProjectPhotoInput:
      type: object
      required: [photoId]
      properties:
        photoId:
          type: integer

    PhotoProjectMembership:
      type: object
      required: [id, name]
      properties:
        id:
          type: integer
        name:
          type: string

    Asset:
      type: object
      required: [id, kind, name, storageKey, contentType, createdAt, updatedAt]
      properties:
        id:
          type: integer
        kind:
          type: string
          enum: [brand, reference]
        name:
          type: string
        variant:
          type: ["string", "null"]
        notes:
          type: ["string", "null"]
        folder:
          type: ["string", "null"]
        projectId:
          type: ["integer", "null"]
        projectName:
          type: ["string", "null"]
        storageKey:
          type: string
        contentType:
          type: string
        filename:
          type: ["string", "null"]
        fileSize:
          type: ["integer", "null"]
        createdById:
          type: ["integer", "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    AssetInput:
      type: object
      required: [kind, name, storageKey, contentType]
      properties:
        kind:
          type: string
          enum: [brand, reference]
        name:
          type: string
          minLength: 1
        variant:
          type: string
        notes:
          type: string
        folder:
          type: string
        projectId:
          type: integer
        storageKey:
          type: string
          description: The objectPath returned by the storage request-url flow (must start with /objects/)
        contentType:
          type: string
        filename:
          type: string
        fileSize:
          type: integer

    AssetUpdate:
      type: object
      properties:
        kind:
          type: string
          enum: [brand, reference]
        name:
          type: string
          minLength: 1
        variant:
          type: ["string", "null"]
        notes:
          type: ["string", "null"]
        folder:
          type: ["string", "null"]
        projectId:
          type: ["integer", "null"]

    AttributionTag:
      type: object
      required: [id, name]
      properties:
        id:
          type: integer
        name:
          type: string

    AttributionTagInput:
      type: object
      required: [name]
      properties:
        name:
          type: string

    PhotoAttributionTagInput:
      type: object
      required: [tagId]
      properties:
        tagId:
          type: integer

    BulkAttributionTags:
      type: object
      required: [ids, tagId, mode]
      properties:
        ids:
          type: array
          items:
            type: integer
        tagId:
          type: integer
        mode:
          type: string
          enum: [add, remove]

    BulkAttributionTagsResult:
      type: object
      required: [updated]
      properties:
        updated:
          type: integer

    AlbumAttributionSummary:
      type: object
      required: [photoCount, tags]
      properties:
        photoCount:
          type: integer
          description: Total photos in the album.
        tags:
          type: array
          items:
            type: object
            required: [id, name, count]
            properties:
              id:
                type: integer
              name:
                type: string
              count:
                type: integer
                description: How many of the album's photos carry this tag.

    AlbumAttributionUpdate:
      type: object
      required: [tagId, mode]
      properties:
        tagId:
          type: integer
        mode:
          type: string
          enum: [add, remove]

    PhotoUpdate:
      type: object
      properties:
        aiDescription:
          type: ["string", "null"]
        takenAt:
          type: ["string", "null"]
        isHidden:
          type: boolean

    SuggestedCollection:
      type: object
      required: [id, title]
      properties:
        id:
          type: integer
        title:
          type: string

    SuggestedNewCollection:
      type: object
      required: [id, suggestedName]
      properties:
        id:
          type: integer
        suggestedName:
          type: string

    SuggestedTag:
      type: object
      required: [name]
      properties:
        name:
          type: string

    SuggestedCategory:
      type: object
      required: [id, name]
      properties:
        id:
          type: integer
        name:
          type: string

    PhotoUploadInput:
      type: object
      required: [url]
      properties:
        url:
          type: string
          minLength: 1
        storageKey:
          type: string
        takenAt:
          type: string
        contentType:
          type: string
        filename:
          type: string
        filesize:
          type: integer

    PhotoTagInput:
      type: object
      required: [tagName]
      properties:
        tagName:
          type: string
          minLength: 1

    PhotoCategoryInput:
      type: object
      required: [categoryId]
      properties:
        categoryId:
          type: integer

    Tag:
      type: object
      required: [id, name]
      properties:
        id:
          type: integer
        name:
          type: string

    TagInput:
      type: object
      required: [name]
      properties:
        name:
          type: string
          minLength: 1

    TagCount:
      type: object
      required: [id, name, count]
      properties:
        id:
          type: integer
        name:
          type: string
        count:
          type: integer

    Category:
      type: object
      required: [id, name]
      properties:
        id:
          type: integer
        name:
          type: string

    CategoryInput:
      type: object
      required: [name]
      properties:
        name:
          type: string
          minLength: 1

    RatingInput:
      type: object
      required: [score]
      properties:
        score:
          type: integer
          minimum: 1
          maximum: 5

    DashboardStats:
      type: object
      required: [totalAlbums, totalPhotos, totalUsers, totalTags, totalCollections, totalProjects, totalPeople, recentActivity]
      properties:
        totalAlbums:
          type: integer
        totalPhotos:
          type: integer
        totalUsers:
          type: integer
        totalTags:
          type: integer
        totalCollections:
          type: integer
          description: Plain collections only (kind='collection') — people are counted separately.
        totalProjects:
          type: integer
        totalPeople:
          type: integer
        recentActivity:
          type: array
          items:
            $ref: "#/components/schemas/Photo"

    UploadUrlRequest:
      type: object
      required: [name, size, contentType]
      properties:
        name:
          type: string
          minLength: 1
          description: Original file name.
        size:
          type: integer
          minimum: 1
          description: File size in bytes.
        contentType:
          type: string
          minLength: 1
          description: MIME type of the file (e.g. `image/jpeg`).

    UploadUrlResponse:
      type: object
      required: [uploadURL, objectPath]
      properties:
        uploadURL:
          type: string
          description: Presigned URL for PUT upload. Absolute in production; may be a relative proxy path (e.g. `/gcs/...`) in local dev.
        objectPath:
          type: string
          description: Normalized object path (e.g. `/objects/uploads/uuid`). Store this in your database.
        metadata:
          $ref: '#/components/schemas/UploadUrlRequest'

    RegistrationSettings:
      type: object
      required: [registrationEnabled]
      properties:
        registrationEnabled:
          type: boolean

    RegistrationSettingsUpdate:
      type: object
      required: [registrationEnabled]
      properties:
        registrationEnabled:
          type: boolean

    AiModelOption:
      type: object
      required: [id, label]
      properties:
        id:
          type: string
        label:
          type: string

    AiProviderInfo:
      type: object
      required: [id, label, model, availableModels, hasKey, envKeyFallbackAvailable, usable]
      properties:
        id:
          type: string
          enum: [openai, anthropic, gemini]
        label:
          type: string
        model:
          type: string
        availableModels:
          type: array
          items:
            $ref: "#/components/schemas/AiModelOption"
        hasKey:
          type: boolean
        keyPreview:
          type: ["string", "null"]
        envKeyFallbackAvailable:
          type: boolean
        usable:
          type: boolean

    AiSettings:
      type: object
      required: [enabled, activeProvider, providers, hasUsableActive]
      properties:
        enabled:
          type: boolean
        activeProvider:
          type: string
          enum: [openai, anthropic, gemini]
        hasUsableActive:
          type: boolean
        providers:
          type: object
          required: [openai, anthropic, gemini]
          properties:
            openai:
              $ref: "#/components/schemas/AiProviderInfo"
            anthropic:
              $ref: "#/components/schemas/AiProviderInfo"
            gemini:
              $ref: "#/components/schemas/AiProviderInfo"

    AiSettingsUpdate:
      type: object
      properties:
        enabled:
          type: boolean
        activeProvider:
          type: string
          enum: [openai, anthropic, gemini]
        providerModels:
          type: object
          properties:
            openai:
              type: string
            anthropic:
              type: string
            gemini:
              type: string

    AiProviderKeyInput:
      type: object
      required: [apiKey]
      properties:
        apiKey:
          type: string
          minLength: 4

    AcceptNewCollectionSuggestionBody:
      type: object
      properties:
        name:
          type: string

    BulkPhotoUpdate:
      type: object
      required: [ids, isHidden]
      properties:
        ids:
          type: array
          items:
            type: integer
          minItems: 1
        isHidden:
          type: boolean

    BulkPhotoUpdateResult:
      type: object
      required: [updated]
      properties:
        updated:
          type: integer

    BulkPhotoDelete:
      type: object
      required: [ids]
      properties:
        ids:
          type: array
          items:
            type: integer
          minItems: 1

    BulkPhotoDeleteResult:
      type: object
      required: [deleted]
      properties:
        deleted:
          type: integer

    BulkRetryAiAnalysisEventsResult:
      type: object
      required: [succeeded, skipped, failed]
      properties:
        succeeded:
          type: integer
        skipped:
          type: integer
        failed:
          type: integer

    AiAnalysisEvent:
      type: object
      required: [id, status, createdAt]
      properties:
        id:
          type: integer
        photoId:
          type: ["integer", "null"]
        photoCaption:
          type: ["string", "null"]
        photoThumbnailUrl:
          type: ["string", "null"]
        provider:
          type: ["string", "null"]
        status:
          type: string
          enum: [success, skipped, failed]
        errorMessage:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time
